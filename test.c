#include <stdio.h>
int main(){
    int count;
    int a = 7;
    int b = 7;

    if (b > 0){
        count = 5;
    } else if (b > 0){
        count = 10;
    }

    printf("%d\n", count);

    return 0;
}

//move the containers
	if (isBlocked){
		if(r0==r1){ //horizontal
			if (countleft > 0){ //left
				
			} else if ( countright > 0){ //right

			}
		}
		if(c0=c1){ //verticle 
			if (countup > 0){ //up
				
			} else if ( countdown > 0){ //down

			}
		}
    }

